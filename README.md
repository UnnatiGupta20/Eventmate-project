

1. Clone the Repository:
git clone "repo link"
cd eventmate

2. Install Dependencies:
npm install

3. Run the Development Server:
npm run dev

4. Open the backend folder in Spring.

5. Change the application.properties file according to your local machine settings.

6. Run the project as a Spring Boot application.
